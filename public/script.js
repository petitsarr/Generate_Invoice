import { FormInput } from "./classes/Form.js";
new FormInput();
/* Submit Form
form.addEventListener("submit" , (e : Event)=>{
    e.preventDefault()

    console.log(`
    ${type.value}
    ${firstname.value}
    ${lastname.value}
    ${address.value}
    ${country.value}
    ${town.value}
    ${zip.value}
    ${product.value}
    ${price.value}
    ${quantity.value}
    ${tva.value}
    `)

})
*/ 
